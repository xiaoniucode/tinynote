<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xilio.tinynote.mapper.ContentMapper">

    <insert id="relationContentMeta">
        insert into relationship(cid,mid)
        values
        <foreach collection="metaIds" item="mid" separator=",">
            (#{cid},#{mid})
        </foreach>
    </insert>
    <update id="setStatusBatchIds">
        UPDATE content
        SET status = #{status},
        updated_at = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
    <select id="selectContentListByMetaId" resultType="cn.xilio.tinynote.controller.theme.dto.PostDetailRes">
        SELECT
            c.id,
            c.title,
            c.summary,
            c.created_at as publishAt
        FROM relationship r
                 INNER JOIN content c ON c.id = r.cid
        WHERE r.mid = #{metaId}
          AND c.status = #{status}
          AND c.draft = 0
        ORDER BY c.created_at DESC

    </select>
</mapper>
